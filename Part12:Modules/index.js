import { Circle } from "./circle.js";

const c = new Circle(10);
c.draw();

/**
 * ES6 Tooling(browser tools)
 * 1. Transpiler
 * -Combination of two words, Translator and Compiler.
 * -Converts modern js code to a code that all our browsers understand.
 * -i.e. Modern -> ES5
 * -e.g. BABEL
 * 2. Bundler
 * -Responsible for combining all js files to a single file called a bundle.
 * -e.g. WEBPACK.
 */
